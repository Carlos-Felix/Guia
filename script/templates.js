var template = Handlebars.compile("<li><h3 id = '{{id}}'>Agregar un repositorio al 'stagin area' (Area de preparación):</h3>
<pre>
    <code><span class='line-code'>git status</span>
        <span class='line-code'>git status --ignored (ver archivos ignorados)</span></code>
</pre>
</li>");